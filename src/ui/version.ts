/**
 * Versión de la app, que viaja dentro de cada archivo `.algx`.
 *
 * Sirve para dos cosas: saber con qué build se hizo una entrega cuando algo
 * sale raro, y poder avisar con precisión si un archivo viene de una versión
 * que esta no sabe leer.
 */
export const APP_VERSION = '1.0.0';
